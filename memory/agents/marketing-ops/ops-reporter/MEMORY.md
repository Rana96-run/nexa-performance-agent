# ops-reporter — memory index

One line per fact, newest on top. Full fact lives in its own `.md` file here.
Formatting rules: `.claude/skills/slack-reporter.md`.

_(empty — add the first formatting edge case when it happens)_
