# designs/ — owned by cro-specialist

Annotated LP design per test (same filename as its brief in `../briefs/`).
Aligned to the OCEAN persona, ZATCA badge above the fold, interaction notes for
the build. As of 2026-06-22, design specs are produced by `cro-specialist` (merged from the former
ui-ux-designer role). The design spec travels with the brief as one package to `developer` (who writes `../specs/`).
