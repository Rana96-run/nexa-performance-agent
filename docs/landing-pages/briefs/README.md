# briefs/ — owned by cro-specialist

One brief per test, from `../_templates/lp-brief-template.md`, named
`YYYY-MM-DD_<product>_<hypothesis-slug>.md`. The brief includes both the 8-section LP spec AND
the annotated design spec (OCEAN-aligned, ZATCA badge, interaction notes). When ready, hand the
complete package to `developer` (who writes `../specs/`). A matching design artifact may also be
written to `../designs/` for reference.
