# briefs/ — owned by cro-specialist

One brief per test, from `../_templates/lp-brief-template.md`, named
`YYYY-MM-DD_<product>_<hypothesis-slug>.md`. When the brief is ready, hand it to
`ui-ux-designer` (who writes the matching file in `../designs/`).
