# specs/ — owned by developer

Build/deploy spec per test (same filename as its brief/design): UTM passthrough
map, both pixel fires, deploy target, and the `../_templates/zatca-checklist.md`
filled in with verified results. On a verified deploy, hand back to
`cro-specialist` to call the test result.
